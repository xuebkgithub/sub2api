/**
 * Settings API endpoints
 * Handles public settings and configuration
 */

// Note: getPublicSettings is defined in @/api/auth to avoid duplication
// Import from there if needed: import { getPublicSettings } from '@/api/auth'

export const settingsAPI = {
  // Future settings-specific functions will be added here
}

export default settingsAPI
